# Environment variables
.env
.env.local
.env.production

# Dependencies
node_modules/

# Logs
logs
*.log

# Uploads folder - include base images, exclude user uploads
# uploads/
# Note: We're including uploads folder so base food images are deployed
# You can exclude specific patterns if needed, like:
# uploads/*.tmp
# uploads/temp/
